# Gateway 进程环境。复制成 .env（已在 .gitignore）。
#
# 注意：进程**不会自动读 .env**。要用它得显式带上：
#   node --env-file=.env --import tsx src/index.ts
# 直接 `pnpm dev` 的话请用 export 或写在命令前面。
#
# 平台级模型密钥只放在 Gateway。不要把这些写到 bot 的磁盘或环境。

# 数据库。必填，没有默认值——起不来会直接告诉你这一条。
# 宿主机端口是 5434（5432 通常已被别的实例占着），容器内互连用 postgres:5432。
GATEWAY_DATABASE_URL=postgres://satuwork:satuwork@127.0.0.1:5434/satuwork
# 多套环境共用一个库时用它隔开；默认 public。
# GATEWAY_PG_SCHEMA=public

# GATEWAY_HOST=127.0.0.1
# GATEWAY_PORT=3080
# GATEWAY_MACHINE_TOKEN=
# GATEWAY_PLATFORM_TOKEN=
# GATEWAY_ACCESS_HOST=satuwork.com
# GATEWAY_PUBLIC_URL=

# 发布包（bot 与机器管家）一律由 CI 上传，凭 GATEWAY_PLATFORM_TOKEN：
#   PUT /platform/bot-releases/:version
#   PUT /platform/manager-releases/:version
# Gateway 自己不打包——包里带 esbuild 的原生二进制，必须在和席位机器同架构的
# Linux 上打，从浏览器或开发机打出来的包装到机器上起不来。
# 上传大小上限，默认 256 MiB。
# GATEWAY_RELEASE_MAX_BYTES=268435456

# 系统管理员。**留空即可**：一个 owner 都没有时，打开首页就是「创建系统管理员」。
# 这两个只给无人值守的自动化用（容器编排、e2e）。
# GATEWAY_OWNER_EMAIL=owner@satuwork.test
# GATEWAY_OWNER_PASSWORD=

# DEEPSEEK_API_KEY=
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# OPENAI_BASE_URL=https://api.openai.com
# ANTHROPIC_BASE_URL=https://api.anthropic.com

# docker compose 用（宿主机映射端口、库名口令）
# POSTGRES_PORT=5434
# POSTGRES_USER=satuwork
# POSTGRES_PASSWORD=satuwork
# POSTGRES_DB=satuwork
