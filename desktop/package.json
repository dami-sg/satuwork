{
  "name": "satuwork-desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "prepare:runtime": "node scripts/prepare-runtime.mjs",
    "pack:runtime-release": "node scripts/pack-runtime-release.mjs",
    "build": "pnpm prepare:runtime && tauri build",
    "icon": "tauri icon"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.0"
  }
}
