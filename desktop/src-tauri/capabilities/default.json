{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "setup",
  "description": "只给打进包里的「连服务器」设置页，允许它读取和保存 Gateway 地址。",
  "windows": ["setup"],
  "permissions": ["core:default", "setup-commands"]
}
